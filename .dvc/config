[core]
    remote = storage
['remote "storage"']
    url = s3://mlops-dvc
    endpointurl = http://localhost:9000
    use_ssl = false
    access_key_id = minio
    secret_access_key = minio12345
    region = us-east-1
